name: CI-Test

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Smoke Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hello
        run: echo "CI works"
